import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#FFF9E5',
    padding: 12,
    borderRadius: 8,
    borderColor: '#FFD700',
  },
  text: {
    fontSize: 16,
    color: '#4a3f0f',
  },
});
