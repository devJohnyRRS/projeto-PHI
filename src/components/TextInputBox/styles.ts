import { StyleSheet, ViewStyle, TextStyle } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    position: 'relative',
    bottom: -225,
  } as ViewStyle,

  icon: {
    position: 'absolute',
    right: 18,
    top: 11,
  } as ViewStyle,
});
